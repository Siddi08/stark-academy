name            = "stark-proxy"
main            = "src/index.ts"
compatibility_date = "2024-11-01"

# Secrets (set via: wrangler secret put ANTHROPIC_API_KEY)
# Then optionally: wrangler secret put ALLOWED_ORIGIN
